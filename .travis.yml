language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t khu-021a/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true khu-021a/docker-react npm run test